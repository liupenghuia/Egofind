# Commands — 已合并

**完整短命令手册与交付口令见：[`CLAUDE.md`](./CLAUDE.md) §7**

本文件仅保留兼容跳转；别名配置仍可在 `product.yaml` → `commands`。

常用：

| 口令 | 含义 |
| --- | --- |
| `顺序完成` / `交付` | 全链路 Orchestrator |
| `产品` / `UI设计` / `架构` / `后端` / `小程序` / `Web` / `测试` | 单角色 |
| `修到绿 <task>` | Fix-to-Green |
| `下一个 <角色>` | 取下一份归属工作 |

产品相关讨论同时遵循 `CLAUDE.md` §2 资深产品经理准则。
