# @egofind/admin-web

React 18 + Vite + Ant Design 5 管理后台。

```bash
pnpm --filter @egofind/admin-web dev
# http://localhost:5173
# 登录 admin / Admin123!
```

页面：登录、数据概览、用户、订单、司机认证、地图预览列表。

`VITE_API_BASE` 默认走 Vite proxy `/api` → `localhost:3000`。
